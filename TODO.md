# TODO

- Split sanitizer policy into two toggles: corruption filtering vs markup stripping, so literal angle-bracket text can be preserved when desired.
- Add settings panel controls for scroll jump behavior: ease multiplier, distance-time influence, base distance rows, and max duration multiplier; persist values in app state.
