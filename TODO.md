# TODO

- [x] Align edit and render viewport (scroll-sync rewrite: single persisted `anchorBlockIndex` per note/snapshot, mode-agnostic restore, see `docs/editor-contract.md`)
- [x] Add render-mode in-note search
- [x] Sort date menu by edited
- [x] `verifyScrollSync.mjs` / `seedLargeNoteAndReload` timing out on a restored note (both now go through `perfHarness.mjs`'s `ensureEditMode`, which switches the pane rather than waiting for a hidden one; `verifyScrollSync` also starts its own dev server now instead of expecting one at a hardcoded 5174, which was the other reason it never ran)
- [ ] **A mode-toggle round trip does not return edit to where it was, and after a scrollbar jump it misses by a mile.** Measured on a 200k-char note (dev:browser, Playwright): a wheel scroll to 312px, toggle to preview and back, returns to 52px. A scrollbar-track click to **31590px**, same round trip, returns to **5356px** -- 26,234px short. Not a regression from the RESTORE_OFFSET_LINES sign fix: the same probe on the previous commit gave 5408px, i.e. identical behavior differing only by that fix's own 2 rows.

  **ROOT CAUSE FOUND, and it is the first step rather than the return.** Reproduced end to end by `scripts/perf/verifyModeToggleRoundTrip.mjs`, committed FAILING as the gate the fix has to pass; its header carries the measurements. Entering render view, the edit pane correctly reports source line 429 (45% of a ~950-line note) and carries it over -- and the preview lands on line 75 (8%). Everything after that is faithful arithmetic on a corrupted input: the return reads the preview's real position, 75, and lands 26,208px adrift, and the caret is not restored either, so ArrowUp slams to the document end. One wrong number, three symptoms.

  `usePreviewMarkdownRendering`'s `scrollPreviewToSourceLine` resolves the block index correctly and calls `virtualizer.scrollToIndex`, which does not converge on a distant block while block heights are still estimates. The retry around it -- `useEditorSectionMount`'s `applyPreviewSourceAnchor` / `attemptFindAndScroll` -- then waits for that block's element to mount, which in a virtualized preview can only happen if the scroll had already arrived. So it spends all ten attempts looking for something that will never appear, and gives up silently.

  **Negative result, so nobody repeats it:** re-issuing the same `scrollToIndex` on each retry does NOT fix this. Tried, measured, reverted -- and the second run showed the first run's apparent improvement was noise. Measurements only improve for blocks that mount, and blocks only mount where the scroll reaches, so re-asking the same question returns the same answer. The fix has to make the landing *verifiable*: scroll, measure where it actually got to, re-aim from there -- which is what the edit side's `settleCorrectionLoop` does and the preview side has no equivalent of.

  Same family as two defects already fixed: the render view's end-of-track miss (a position resolved against estimated geometry and trusted without checking) and the source-line/visual-row confusion behind the note-switch drift. Worth reading together when the scrolling rules get written down.

- [ ] **A first click into a non-active section marks it active without giving it working focus** (user-reproducible every time; suspected to be behind the "left/primary editor steals focus" reports, and worth fixing early because a mis-focused pane quietly corrupts any other scroll/caret diagnosis run near it). Repro: two sections, left in render view and ACTIVE having just been scrolled with the arrow keys, right in edit mode. One click on the right editor: it shows as active, but no caret appears even though its position is inside the viewport, and the arrow keys no longer move the caret -- they nudge the text up and down by a fraction of a row, clamped, as if something is scrolling rather than navigating. A SECOND click restores normal behavior.

  **Measured, user-supplied:** `document.activeElement` is `body` after the first click and `div.cm-content.editor-text.cm-lineWrapping` after the second. So focus is genuinely never applied on the first click -- this is focus application, not key handling, and the arrow-key nudge is just what arrow keys do when nothing is focused.

  **Narrowed by probe:** with BOTH sections in edit mode and focus deliberately dropped to `body` first, a single click into the inactive section focuses it correctly (`cm-focused` = 1), and the `.cm-content` node is *not* replaced across activation (tagged and compared). So it is neither generic first-click behavior nor a DOM remount. The remaining ingredient is the other section being in RENDER view, which is exactly the precondition the Playwright harness could not set up -- neither the per-section toggle button nor Escape switches a column's mode from a script, so this needs the real app or a harness that can drive mode switching.

  **A guard is in place, and it is paint, not a cure** (`EditorSection.tsx`, the `wasSectionActiveRef` effect): a section entering the active state in edit mode, with focus not already inside it, now schedules `scheduleFocusEditorInEditMode`. It closes a real gap on its own terms -- `markSectionActive` only sets `activeSectionId`, so nothing anywhere put focus in the newly active section, and activation should never be able to disagree with focus. But it is NOT verified to fix the reported defect: no harness can construct the state the defect needs, and with the guard stashed the same probe passes identically, so nothing demonstrates the guard changing behaviour. **Keep this item open.** If the symptom returns, the guard means it will look different (focus may arrive late rather than never), so re-measure before assuming anything.

  **Also worth knowing:** it stopped reproducing after an app restart, which says the trigger is accumulated session state rather than a fixed code path -- consistent with every harness, which always starts clean, failing to reproduce it.

  **Ruled out so far**, each by measurement rather than reasoning: DOM replacement (the `.cm-content` nodes are tagged and identical across activation); generic first-click behaviour (one click focuses correctly in both harnesses); a timing race against the render pane's arrow-scroll animation (clicking at 0ms, 150ms and 1500ms after the arrows all focus correctly); and the runtime itself (the real Electron app, driven with an isolated `--user-data-dir`, also focuses on the first click). So the trigger is something in a real session's state that a freshly-seeded harness does not have.

  **Instrument for it: `src/dev/focusDiagnostics.ts`.** `localStorage.setItem('thockdown:debug-focus', '1')`, reload, perform the gesture, and read the `[focus]` lines: every mousedown in the capture phase with its `defaultPrevented`, what holds focus one frame and one tick later, and every focusin/focusout with both parties and their section column. A `focusout ... to=null` names whatever is pulling focus back to `<body>`; no focusin at all after the mousedown means focus was never applied rather than applied and stolen. Costs nothing when the flag is off -- the listeners are not attached.

  `markSectionActive` (App.tsx) only calls `setActiveSectionId`; nothing focuses the newly active section's editor. `scheduleFocusEditorInEditMode` exists and is called on MODE changes (the render->edit fast path) but never on a section-activation change. A guarded self-healing fix -- on `isSectionActive` becoming true, in edit mode, with focus not already inside this section, schedule that same focus -- is the obvious candidate, but it compensates rather than cures and should not be shipped until the render-view interaction is actually understood and reproducible.

- [ ] Refine editor input focus behavior
- [ ] `resolveIdentityLabel`'s derived-snippet fallback is now unreachable in practice: every note and every chapter carries an id (assigned at birth, and backfilled at startup by `sanitizeDatabase` for anything older). It is kept as a defensive floor rather than deleted -- the branch costs nothing, and removing it means changing a shared signature plus the tests that document it. Delete only if something else forces a change there.
- [ ] `DatabaseService.sanitizeDatabase()` (startup notes_fts dedupe + conditional VACUUM, see `docs/large-document-performance-handover.md`'s newest session) has only been manually verified once against one real database — watch for its `[db] startup sanitation` log across a few real launches before considering it fully trusted.
- [ ] Cosmetic only: ~19 of the `scripts/perf/verifyCM6*.mjs`/`measureCM6*.mjs` scripts still set the now-vestigial `localStorage['thockdown:cm6-editor-spike'] = '1'` flag before launching (harmless no-op now that CM6 is the only editor and the flag does nothing — Lexical and its rollback path were fully removed). Not touched during that removal since it's pure cleanup with zero behavior change; strip the lines whenever one of these scripts is next touched for real.
- [ ] On a full app restart, note text in the editor isn't aligned to the grid on initial load (both open sections, if split) — clicking into either editor section fixes it for both. Never root-caused; likely a measurement/layout timing issue on cold mount. The scroll-sync rewrite removed `applySourceAnchorToEditor` (the previously-suspected related mechanism) entirely, so re-check whether this still reproduces before investigating further -- if it doesn't, that was likely the cause.
- [ ] The render view's scroll bridge spoofs the Universal Declaration of Human Rights (`src/assets/hrd.md`, via `scrollBridgeTexture.ts`). It is public-domain UN text and it is never readable at the speed it passes, but it is quoted at length all the same -- it needs an attribution line. Not in the User Guide: the guide tells a user how to work the app, and the bridge is not something they operate (see the note on guide scope in `CLAUDE.md`). Put it in the design-philosophy document below, whenever that gets written.

- [ ] Write a design-philosophy document -- a separate thing from the User Guide, and explicitly not user instructions. Read as a sales pitch for an app that is not for sale: what this app's design actually commits to, worked examples of those commitments being honored, and the blend of features that makes it unlike its neighbours. Home for everything that is interesting-but-not-instructional: the scroll bridge and the Declaration passing on it, why there are no context menus anywhere, why chapters cannot do what folders do. Some of this exists in scattered form in `docs/guiding-vision.md` (agent-facing) and in the interaction/workflow contracts under `docs/` -- this would be the outward-facing counterpart.

## Split-view rough edges (carried over from split-view handover doc)
- [ ] `editorStageRef` (App.tsx) is one shared ref across all sections for the background-texture-sizing `ResizeObserver`; it only ever tracks whichever section's stage DOM node mounted/updated last.
- [ ] `pendingViewportRestoreRef`/`isApplyingInitialViewportRef` are shared across sections, so one section's viewport-restore-in-progress window can transiently suppress another's save.
- [ ] In `npm run dev:browser`, `appShellWidthPx` was once observed stuck at a stale, too-narrow value after a sequence of interactions, hiding the "+" button even with room. Never root-caused; may be a browser-mock-only artifact.
- [ ] Hibernation rendering has never been exercised with real N>1 typing load — confirm inactive sections actually stop wiring live-typing listeners.
- [ ] Cold-start restore with 2+ sections has only been verified via browser-mock reloads, not an actual Electron app restart.
